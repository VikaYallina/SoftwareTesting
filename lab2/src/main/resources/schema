DROP TABLE IF EXISTS springboot.users;

CREATE TABLE springboot.users (
                                 username VARCHAR(45) NOT NULL,
                                 password VARCHAR(45) NULL,

                                 PRIMARY KEY (username));